#stories
	- @artists.each do |artist|
		%h5= artist.name
		- artist.stories.featured.each do |story|
			.story
				=raw story.story
				.story-date= story.date.strftime("%m.%d.%Y")
			
	
	=link_to 'See news archives >', '/archives' 