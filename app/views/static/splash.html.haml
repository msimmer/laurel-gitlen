#splash
	#splash-wrap
		#splash-content
			%strong=raw date_markup(@exhibition, true)
			%br
			=link_to @exhibition.name, exhibition_url(@exhibition, slide: 0), data: {remote: true}
			-unless @exhibition.artists.blank? || @exhibition.artists.count > 3
				%br
				&mdash;
				%br
				=raw @exhibition.artists.map{ |a| a.name }.join('<br/>')