.exhibition-images
	-unless exhibition.pieces.blank?
		-exhibition.piece_order.each_with_index do |id, index| 
			=link_to image_tag(exhibition.pieces.find(id).image.url(:thumb)), exhibition_url(exhibition, slide: index)