$('<%=j render "gallery" %>').appendTo("body");

$('#gallery-slides').cycle({
	prev: $('#prev'),
	next: $('#next'),
	timeout: 0
});

$('.close').click(function(){
	$("#gallery").fadeOut(function(){
		$(this).remove();
	});
});